title: Avi fixes
body:
Fixes the AVI caching logic to be more careful when it uses a cached plane (e.g. if the plane's extents are less than those requested).

NB: depends on the N-D branch being merged first
number: 71
milestone: null
labels:
merged: false
comments:
	merged via command line.
